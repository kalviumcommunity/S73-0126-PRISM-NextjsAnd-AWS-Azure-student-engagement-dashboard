During migration, Prisma initially failed to authenticate because the CLI runs on the host machine and requires a localhost database connection. The issue was resolved by configuring a separate DATABASE_URL for local Prisma migrations